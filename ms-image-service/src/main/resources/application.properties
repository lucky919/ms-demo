# service name
spring.application.name=images-ws

# port - use random
server.port=${PORT:0}

# eureka server 
eureka.client.service-url.default.zone=http://localhost:8761/eureka

# dynamic instance id 
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

